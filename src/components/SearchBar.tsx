import { SafeAreaView, Text } from 'react-native'
import React from 'react'
import {useNavigation} from '@react-navigation/native'
// TODO: Consulta, cosas que tengo como "use navigation" y "safeareaveiw" tengo que copipastearlas o las puedo poner en otro lado para evitar?


const SearchBar = () => {
  return (
    <SafeAreaView>
      <Text>SearchBar</Text>
    </SafeAreaView>
  )
}

export default SearchBar